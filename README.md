# brnyos
OS Project im doing
## How to build
Prerequisites ```gcc, make, nasm```
To build, run 
```
make
```
in the root directory of the repository.
To also build the fat tools (optional) run
```
make tools_fat
```
An image file will be available in the build folder.
It has been tested in qemu only.
